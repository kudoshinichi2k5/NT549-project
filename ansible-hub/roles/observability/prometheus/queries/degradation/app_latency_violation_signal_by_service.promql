(
  histogram_quantile(
    0.95,
    sum by (service, le) (
      rate(http_server_requests_seconds_bucket{job="mini-ecommerce-actuator",uri!="/actuator/prometheus"}[1m])
    )
  ) > bool 0.5
)
or on(service) (0 * max by (service) (up{job="mini-ecommerce-actuator"}))
