clamp_min(
  (sum(node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes) / (1024^3)) -
  (
    (sum(sum by (service) (jvm_memory_used_bytes{job="mini-ecommerce-actuator"})) / (1024^3))
    or vector(0)
  ),
  0
)
or vector(0)
