apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.yaml
  - service.yaml
  - statefulset.yaml

configMapGenerator:
  - name: user-db-init
    files:
      - init-users.sql
