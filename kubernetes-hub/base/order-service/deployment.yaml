apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
spec:
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      serviceAccountName: order-service
      containers:
        - name: order-service
          image: tienphatng237/mini-ecommerce-order-service
          imagePullPolicy: Always
          ports:
            - containerPort: 8080

          env:
            - name: DB_URL
              value: jdbc:postgresql://order-db:5432/orderdb
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: order-db-secret
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-db-secret
                  key: POSTGRES_PASSWORD
            - name: PRODUCT_SERVICE_BASE_URL
              value: http://product-service:8080
            - name: INTERNAL_SERVICE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: auth-jwt-secret
                  key: INTERNAL_SERVICE_TOKEN
            - name: REDIS_HOST
              value: redis
            - name: REDIS_PORT
              value: "6379"
            - name: HTTP_CONNECT_TIMEOUT_MS
              value: "2000"
            - name: HTTP_READ_TIMEOUT_MS
              value: "5000"
